spring:
  config:
    import: security-variables.yml
  application:
    name: Marondalgram
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/marondalgram_250922?serverTimezone=Asia/Seoul
    username: root
    password: ${mysql.password}

mybatis:
  mapper-locations: mappers/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true